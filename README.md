# dazense docs

Mintlify documentation site for Dazense.

Local structure:
- `docs/` content pages
- `docs.json` site configuration
- `assets/` logos and favicon

To publish, connect this repo to Mintlify and set the custom domain to `dadocs.metazense.com`.
